# quant-factors

Shared factor computation library.

## Commands

```bash
pip install -e ".[dev]"
quant-factors list
quant-factors compute --config configs/example.yaml
pytest -q
ruff check src tests
```
